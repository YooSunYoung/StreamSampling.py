# Installation

To install StreamSampling and all of its dependencies, use

`````{tab-set}
````{tab-item} pip
```sh
pip install streamsampling
```
````
````{tab-item} conda
```sh
conda install -c conda-forge streamsampling
```
````
`````
